import type { Lang } from "../lang";
import { ControllerBase } from "./ControllerBase";
export interface BasicAuth {
    username: string;
    password: string;
}
export default class PluginSystemController extends ControllerBase {
    private ui?;
    private getUI;
    private isReactPage;
    protected goToPluginSystemPage(baseUrl: string): Promise<import("puppeteer").HTTPResponse | null>;
    readyForUpload(options: {
        baseUrl: string;
        userName: string;
        password: string;
        lang: Lang;
        basicAuth?: BasicAuth;
    }): Promise<void>;
    upload(pluginPath: string, lang: Lang): Promise<void>;
}
